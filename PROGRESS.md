# Progress Log

Append a new dated entry at the top each time work is done. Keep entries short and factual.

---

### 2026-09-24 — Admin panel v2: add blog posts, accent color, logo upload
- Site owner asked in chat where the previously-requested-but-unbuilt items stood, and picked "Admin panel v2" (from ROADMAP's NEEDS HUMAN section) to tackle now. Built all three remaining capabilities in one pass, reusing `api/_lib/github.js` and `api/_lib/auth.js` (`requireAuth`) unchanged, per the design notes already left in ROADMAP.md.
- **Add a Blog Post** (`api/admin/blog-post.js` + `api/_lib/blogPost.js`): the admin fills in an English and Arabic title/meta-description/body and an optional related-tool slug; the endpoint slugifies the title, formats the date bilingually (including Eastern Arabic-Indic digits and Arabic month names, matching the site's existing convention), converts each body from a small safe "lite markdown" (blank-line paragraphs, `## ` headings, `- ` bullet lists — every character HTML-escaped before any tag wraps it, so it can never emit raw markup) into HTML, then commits the new post page, lists it on the blog index, swaps it into the homepage's "From the blog" two-post strip (dropping the oldest shown post), and adds it to the sitemap. Rejects a duplicate slug (409) and a related-tool slug that doesn't actually exist (400) before writing anything.
- Per ROADMAP's explicit warning, this does **not** touch `assets/i18n.js`. Instead added two small, generic, dictionary-free mechanisms to `i18n.js`'s `applyLang()`: `data-lang="en"|"ar"` (shows/hides a whole block per active language — used for the post body) and `data-lang-en="..."`/`data-lang-ar="..."` (swaps a short string — used for `<title>`, `<meta description>`, `<h1>`, the date line, and the card's title/description/date). Both are reusable by any future self-contained bilingual page, not just blog posts.
- **Accent Color** (`api/admin/theme-color.js` + `api/_lib/themeColor.js`): GET returns the current `--accent` value from `assets/style.css`; POST validates a 6-digit hex color and regex-replaces only that one custom property, leaving `--accent-2` and everything else untouched.
- **Logo** (`api/admin/logo.js` + `api/_lib/imageUpload.js`): the admin panel's browser-side JS draws any uploaded image onto a canvas and re-exports it as PNG (same "conversion happens client-side" pattern as the Image Converter tool) before sending it; the endpoint verifies the PNG file signature and a 2 MB size cap before committing to `assets/logo-icon.png` (using the existing file's `sha` so it's an update, not a duplicate-create error). Favicon regeneration stays a manual follow-up, as the design notes anticipated.
- Extended `admin/index.html`'s dashboard with three new panels (Add a Blog Post, Accent Color, Logo) replacing the old "Coming next to this panel" placeholder, wired to the three new endpoints with the same fetch/error-handling conventions as the existing Show/Hide panel.
- Added two small marker comments (`<!-- grid:end -->` in `blog/index.html`, `<!-- blogstrip:start/end -->` around the homepage's blog-teaser grid) so the new insertion logic can find exactly where to add/replace cards without risking the tool cards elsewhere on the same pages.
- Verification (no live Vercel/GitHub environment available from this session, so tested as thoroughly as possible short of that): `node --check` on every new/changed `.js` file; unit tests of every pure function in `blogPost.js`/`themeColor.js`/`imageUpload.js` (slugify, bilingual date formatting, lite-markdown escaping incl. an XSS attempt, card insertion, sitemap insertion); a full end-to-end simulation of all three endpoints with a mocked `fetch` standing in for the GitHub Contents API (seeded with the real repo files) — covering the happy path, duplicate-slug rejection (409), a related-tool slug that doesn't exist (400), a non-PNG/wrong-mime logo upload (400), and an invalid hex color (400); a repo-wide i18n key-coverage check (zero missing, 629 keys); HTML tag-balance checks on every changed file; and a Playwright smoke test of the generated post page, blog index and homepage (all load with zero console errors, and `#lang-toggle` correctly swaps the new `data-lang`/`data-lang-en/ar` content to Arabic and flips to RTL, alongside the existing `data-i18n` chrome).
- `ADMIN_SETUP.md` updated to document all three new capabilities and how they work.

### 2026-09-24 — 11th blog article: "Celsius to Fahrenheit Conversion: The Formula, a Chart, and Why It's Not Just Multiplication"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up stays deferred (same real-corruption-risk reasoning as every prior run since 2026-09-05). Checked the CSV-to-JSON candidate idea named in ROADMAP.md first — confirmed by reading `tools/json-formatter/index.html` that it's JSON-only (format/minify/validate) with no CSV handling at all, so skipped that idea as instructed rather than building it out. Picked a fresh long-tail topic instead: the Unit Converter tool (temperature/length/weight, shipped 2026-09-16) had no companion blog article yet, and "Celsius to Fahrenheit" is a genuinely high-volume, evergreen search query.
- Added `blog/celsius-to-fahrenheit-conversion/index.html` (~650 words): gives the real formula (°F = °C × 9/5 + 32) and explains what each part of it does and why, a quick-reference chart of commonly-searched temperatures (freezing, room temp, body temp, oven temp, boiling), the reverse Fahrenheit-to-Celsius formula, and the historical reason the two scales don't share a zero point (Fahrenheit's brine/body-temperature origins predating Celsius's water-based decimal scale), plus a mention of how Kelvin sidesteps the problem entirely. Verified against the tool's actual implementation (`tools/unit-converter/index.html`'s `toCelsius`/`fromCelsius` functions) before writing the CTA copy.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 11 posts), swapped it into the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 10 and 11, the two most recent), added its URL to `sitemap.xml`, added `blog.post11.*` and `blogpost11.*` keys with full English and Arabic translations to `assets/i18n.js`.
- Verified before committing: a Node/vm script diffing every `data-i18n*` key across all `.html` files against `assets/i18n.js`'s `en`/`ar` dictionaries (573 keys used, zero missing in either language, 658 keys defined in each), `node --check assets/i18n.js` for JS syntax, a Python tag-balance check on the new/changed files, and a Playwright smoke test on the new post, the blog index and the homepage (all three load with zero console errors, and `#lang-toggle` correctly flips to `dir="rtl"` and swaps the page title/content on all three).
- Note: this entry landed in the repo concurrently with the same day's "Admin panel v2" work above (a separate session/run) — both are independent, non-overlapping changes, merged here without conflict beyond ordering in this log and the shared `blog/index.html`/`index.html`/`sitemap.xml` insertion points.

### 2026-09-23 — 10th blog article: "How to Check If a Password Has Been Leaked (and What Makes One Actually Strong)"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up stays deferred (same real-corruption-risk reasoning as every prior run since 2026-09-05), so moved to the next well-scoped item — the "how to check if a password has been leaked / what makes a password strong beyond length" article already named as a candidate in ROADMAP.md, a deeper companion to the existing "how to generate a strong password" post and the Password Generator tool.
- Added `blog/how-to-check-if-a-password-has-been-leaked/index.html` (~700 words): explains what actually makes a password strong beyond length (randomness across a large character set, uniqueness — leetspeak substitutions barely slow down modern crackers), how breaches actually happen (leaked databases, credential-stuffing attacks against reused passwords), how to check a password against Have I Been Pwned's "Pwned Passwords" database safely (explains k-anonymity — only a SHA-1 hash prefix is sent, never the password itself), and what to do if a password turns up leaked (change it everywhere it was reused, enable 2FA, use a password manager). Checked the Password Generator tool's actual feature set (length slider, character-set checkboxes, exclude-similar option) before writing the CTA copy. Does not build a new tool or call any external API from the site itself — purely educational content, consistent with the "no backend" architecture.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 10 posts), swapped it into the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 9 and 10, the two most recent), added its URL to `sitemap.xml`, added `blog.post10.*` and `blogpost10.*` keys with full English and Arabic translations to `assets/i18n.js`.
- Verified before committing: a Node/vm script diffing every `data-i18n*` key across all `.html` files against `assets/i18n.js`'s `en`/`ar` dictionaries (545 keys used, zero missing in either language), `node --check assets/i18n.js` for JS syntax, a Python tag-balance check on the new/changed files, and a Playwright smoke test on the new post, the blog index and the homepage (all three load with zero console errors, and `#lang-toggle` correctly flips to `dir="rtl"` and swaps the page title/content on all three).

### 2026-09-22 — 9th blog article: "What Is Base64 Encoding and When Do You Actually Need It?"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up stays deferred (same real-corruption-risk reasoning as every prior run since 2026-09-05), so moved to the next well-scoped item — the "what is Base64 encoding and when do you actually need it" article already named as a candidate in ROADMAP.md, pairing with the Base64 Encoder & Decoder tool (shipped 2026-09-10), which hadn't gotten a companion article yet.
- Added `blog/what-is-base64-encoding/index.html` (~600 words): explains what Base64 actually does (packing 3 bytes into four 6-bit printable characters, ~33% size overhead), directly corrects the two most common misconceptions (it is not encryption — fully and instantly reversible with no key — and not compression — output is larger, not smaller), covers real use cases (reading JWT payloads, small-image data URIs, email/MIME attachments, passing binary data through text-only fields), and a "when you probably don't need it" section (passwords need real hashing, not Base64; large images should stay real files, not inflated data URIs), ending with a 4-item quick-reference list. Checked the Base64 Encoder & Decoder tool's actual feature set (text encode/decode plus a separate file-to-data-URI converter) before writing the CTA copy.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 9 posts), swapped it into the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 8 and 9, the two most recent), added its URL to `sitemap.xml`.
- Added the full `blogpost9.*` and `blog.post9.*` key set to `assets/i18n.js` in both `en` and `ar` sections (Arabic written in the same plain-language Gulf-dialect style as the existing posts, matching the site's established "Base64 = تشفير Base64" / "فك تشفير" terminology from the tool page). Verified with a Node script that every `data-i18n*` key used across all `.html` files has both an `en` and `ar` entry (605/605 keys matched, 0 missing) and that `assets/i18n.js` parses with `node --check`.
- Ran a Playwright headless smoke check against the new post, `blog/index.html` and `index.html`: all three load with zero console/page errors, and `#lang-toggle` correctly flips `dir` to `rtl` and swaps visible text (including the new post's Arabic title) on each page.

### 2026-09-21 — 8th blog article: "How to Convert a Unix Timestamp to a Readable Date"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run since 2026-09-05), so moved to the next well-scoped item — another long-tail blog article, this time the "how to convert a Unix timestamp to a readable date" idea already named as a candidate in ROADMAP.md, pairing with the Timestamp Converter tool (shipped 2026-09-16), which hadn't gotten a companion article yet.
- Added `blog/unix-timestamp-to-readable-date/index.html` (~650 words): explains what a Unix/epoch timestamp actually counts (seconds since Jan 1 1970 UTC), the seconds-vs-milliseconds mix-up (10 vs. 13 digit numbers) that's the most common source of confusion, why the timestamp itself carries no timezone (only its display does), and briefly covers the Year 2038 32-bit overflow problem, ending with a 4-item quick-reference list. Links to the existing Timestamp Converter tool; checked the tool's actual feature set (seconds/milliseconds auto-detection, local/UTC/ISO 8601/relative-time output, Date→Timestamp section) before writing so the CTA copy matches what it really does.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 8 posts), swapped it into the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 7 and 8, the two most recent), added its URL to `sitemap.xml`.
- Fully bilingual: added `blog.post8.*` (card copy) and `blogpost8.*` (full post copy) keys to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic prose in the same Gulf-colloquial register as the existing posts, not machine-translated). Verified with a Node/vm-style script that evaluates the `translations` object literal directly and diffs it against every `data-i18n`/`data-i18n-content`/`data-i18n-placeholder` key used across all HTML pages: 581 keys in both dictionaries, all 499 used keys present across all 29 HTML pages, zero missing either direction.
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a Python HTML open/close tag-balance check across all 29 pages (zero issues), and a Playwright smoke test served over a local `http.server` — the new post loads with zero console errors, its H1 and CTA href are correct, `#lang-toggle` flips `dir` to `rtl` and swaps the H1 to the Arabic title, clicking the CTA navigates through to `tools/timestamp-converter/index.html`, the blog index shows 8 cards, and the homepage "From the blog" strip shows exactly 2 cards (the two newest posts) — zero console errors across every page checked (homepage, blog index, new post, timestamp-converter tool).

### 2026-09-19 — 7th blog article: "UUID vs GUID"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run since 2026-09-05), so moved to the next well-scoped item — another long-tail blog article, this time the "UUID vs GUID" idea already named as a candidate in ROADMAP.md, pairing with the UUID/GUID Generator tool (shipped 2026-09-14), which hadn't gotten a companion article yet.
- Added `blog/uuid-vs-guid-whats-the-difference/index.html` (~700 words): explains that UUID (the RFC 4122 standard term) and GUID (Microsoft's term for Windows/.NET/SQL Server) are structurally the same 128-bit value, covers the actual source of variation — version numbers (v1 timestamp+MAC, v4 random, v5 deterministic namespace-hash) — explains when you genuinely need one (independent ID generation across multiple servers/offline clients with no central coordinator, hard-to-guess tokens/session IDs), and ends with a 4-item quick decision list. Links to the existing UUID/GUID Generator tool.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 7 posts), swapped it into the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 6 and 7, the two most recent), added its URL to `sitemap.xml`.
- Fully bilingual: added `blog.post7.*` (card copy) and `blogpost7.*` (full post copy) keys to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic prose in the same Gulf-colloquial register as the existing posts, not machine-translated). Verified with a Node/vm key-coverage script (557 keys in both dictionaries, all 476 `data-i18n*` keys used across all 28 HTML pages present in both languages, zero missing).
- Sanity-checked before commit: `new Function(...)` syntax check on `assets/i18n.js`, a Python HTML open/close tag-balance check across all 28 pages (all balanced), and a Playwright smoke test served over a local `http.server` — the new post loads with zero console errors, `#lang-toggle` flips `dir` to `rtl` and swaps the H1 to the Arabic title, its CTA link resolves to and navigates through to `tools/uuid-generator/index.html`, the blog index shows 7 cards and its new card click-through resolves correctly, and the homepage "From the blog" strip shows exactly 2 cards (the two newest posts) — zero console errors across every page checked (homepage, blog index, new post, UUID generator tool).

### 2026-09-18 — 6th blog article: "HEX vs RGB vs HSL"
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run — this is unchanged from every session since 2026-09-05), so moved to the next well-scoped item — a long-tail blog article, this time pairing with the newest tool (color-converter, shipped 2026-09-17) rather than an older one, since it hadn't gotten a companion article yet.
- Added `blog/hex-vs-rgb-vs-hsl/index.html` (~650 words): explains what each of the three color formats actually encodes (HEX as compact base-16 RGB, RGB as the same channels in base-10, HSL as hue/saturation/lightness — the format built for adjusting rather than just recording a color), why converting between them is lossless pure arithmetic, and a 4-item quick decision list. Links to the existing Color Converter tool; checked the tool's actual field set (HEX/RGB/HSL, random-color button, live sync) before writing so the CTA copy matches what it really does.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid (now 6 posts), added it to the homepage's "From the blog" two-card strip in place of the oldest shown post (now shows posts 5 and 6, the two most recent), added its URL to `sitemap.xml`.
- Fully bilingual: added `blog.post6.*` (card copy) and `blogpost6.*` (full post copy) keys to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic prose in the same Gulf-colloquial register as the existing posts, not machine-translated). Verified with a Node/vm key-coverage script (533 keys in both dictionaries, all 453 `data-i18n*` keys used across all 27 HTML pages present in both languages, zero missing).
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a Python HTML open/close tag-balance check on the new and changed files (all balanced), and a Playwright smoke test served over a local `http.server` — the new post loads with the correct H1 and its CTA link resolves to `tools/color-converter/index.html`, `#lang-toggle` flips `dir` to `rtl` and swaps the H1 to the Arabic title, the blog index shows 6 cards, and the homepage "From the blog" strip shows exactly 2 cards (the two newest posts) — zero console errors across all three checked pages.

### 2026-09-17 — 13th tool: HEX/RGB/HSL Color Converter
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run), so moved to the next well-scoped item — the HEX/RGB/HSL color converter flagged in the 2026-09-16 entry as "a good candidate for the next 'new tool' increment."
- Added `tools/color-converter/index.html`: a native `<input type="color">` picker plus three text fields (HEX, RGB, HSL) that all stay in sync in both directions — editing any one of the four (picker, HEX, RGB, HSL) instantly recalculates the other three and updates a live color-swatch preview. Includes a "Random color" button and a per-field Copy button. Conversion math (RGB↔HSL, hex parsing/formatting, tolerant parsing of `rgb(...)`/`hsl(...)` strings or bare comma-separated numbers) is plain JS, zero dependencies. Invalid input in any text field shows a plain-language status message without clobbering the other fields' last-good values.
- Wired in everywhere new tools go: 13th card on `index.html`'s tool grid (🎨 icon), URL added to `sitemap.xml`, reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (what HEX/RGB/HSL each mean and why they differ / when you'd actually need to convert between them).
- Fully bilingual: all new `color.*` and `home.card.color.*` keys added to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic copy, not machine-translated). Verified with a Node/vm key-coverage script: 509 keys in both dictionaries, all 430 `data-i18n*` keys used across all 26 HTML pages present in both languages, zero missing.
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a JS syntax check (`new Function(...)`) on the new page's inline script, a Python HTML open/close tag-balance check across every page (zero issues), and a Playwright smoke test served over a local `http.server` — verified the default on-load example color, typing a HEX value correctly recalculates RGB and HSL, typing an RGB string recalculates HEX and HSL, typing an HSL string recalculates HEX and RGB, an invalid HEX value shows the "not a valid color" status message, the Random button changes all three fields together, the Copy button puts the exact field value on the real clipboard, driving the native color-picker input recalculates the text fields, and `#lang-toggle` flips to RTL Arabic with the H1 translated — zero console errors throughout. Also confirmed the homepage tool-grid card count went from 12 to 13 with the new card's link resolving correctly.

### 2026-09-16 — 12th tool: Unix Timestamp Converter
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run), so moved to the next actionable item — one of the two developer-utility tool candidates listed ("Unix timestamp ↔ human-date converter, and a HEX/RGB/HSL color converter"). Built the timestamp converter, leaving the color converter as the next candidate.
- Added `tools/timestamp-converter/index.html`: a live, auto-updating current-Unix-timestamp display with a copy button, a "Timestamp → Date" section (paste a timestamp, auto-detects seconds vs. milliseconds by magnitude or set it manually, outputs local time, UTC time, ISO 8601, and a plain-language relative time via `Intl.RelativeTimeFormat`), and a "Date → Timestamp" section (a `datetime-local` picker with a Local/UTC interpretation toggle, outputs Unix seconds, milliseconds, and ISO 8601). Every output row has its own copy button. Both sections show a working example immediately on load (current time) rather than starting empty. Zero dependencies — plain JS `Date`/`Intl` APIs only.
- Wired in everywhere new tools go: 12th card on `index.html`'s tool grid (🕒 icon), URL added to `sitemap.xml`, reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (what a Unix timestamp is / when you'd use a converter).
- Fully bilingual: all new `ts.*` and `home.card.ts.*` keys added to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic copy, not machine-translated). The relative-time formatter also uses the current UI language (`ar` or `en`) so "3 hours ago" renders as "قبل ٣ ساعات" and similar when the page is toggled to Arabic. Verified with a Node/vm key-coverage script: 491 keys in both dictionaries, all 414 `data-i18n*` keys used across all 25 HTML pages present in both languages, zero missing.
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a JS syntax check (`new Function(...)`) on the new page's inline script, a Python HTML open/close tag-balance check on the new and changed files (both balanced), and a Playwright smoke test served over a local `http.server` — verified the live ticking timestamp and its copy button (real clipboard content matched), a known timestamp (1700000000) converting to the correct UTC/ISO date, correct auto-detection when the same instant is pasted in milliseconds (1700000000000), the "enter a valid whole number" error message on non-numeric input, the Date→Timestamp section correctly converting a UTC-interpreted date/time back to 1700000000, `#lang-toggle` flipping to RTL Arabic with the H1 translated, and the homepage tool-grid card count going from 11 to 12 with the new card's link resolving correctly — zero console errors throughout.

### 2026-09-15 — 5th blog article: PNG vs JPEG vs WebP
- Picked the next actionable ROADMAP item: the PDF raw/FlateDecode image-recompression follow-up at the top stays deferred (same real-corruption-risk reasoning as every prior run), so moved to the next item — the previously-flagged blog article candidate "PNG vs JPEG vs WebP — which to use and when."
- Added `blog/png-vs-jpeg-vs-webp/index.html`: ~700 words covering when JPEG's lossy compression is right (photos), when PNG's lossless/transparency support is right (logos, screenshots, diagrams), why WebP is usually the best default for the web (smaller than JPEG at equal quality, plus lossless+transparency support like PNG) with an honest compatibility caveat for very old software, and a 4-item quick decision list. Links to the existing Image Converter & Compressor tool, whose actual supported formats (JPEG/WebP/PNG) were checked in its source before writing the content so the claims match what the tool really does.
- Wired in everywhere a new post goes: added its card to `blog/index.html`'s grid, added it to the homepage's "From the blog" strip (replacing the oldest of the two shown, so it still shows the latest two posts), added its URL to `sitemap.xml`.
- Fully bilingual: added `blog.post5.*` (card copy) and `blogpost5.*` (full post copy) keys to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic prose, matching the tone of the existing posts, not machine-translated). Verified with a Node/vm key-coverage script: 455 keys in both dictionaries, all 389 `data-i18n*` keys used across all 24 HTML pages present in both languages, zero missing.
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a Python HTML open/close tag-balance check across every page (zero issues), and a Playwright smoke test served over a local `http.server` — new post page loads with the correct H1, CTA link resolves to the image-converter tool, `#lang-toggle` flips `dir` to `rtl` and swaps the H1 to the Arabic title, the homepage now links to the new post and no longer shows the JSON-invalid post in its "From the blog" strip, and zero console errors across the new post, the blog index, the homepage and the sitemap.

### 2026-09-14 — Admin login + control panel v1 (real backend, since hosting migrated to Vercel)
- Owner confirmed the Vercel migration was working and asked directly in chat to build the admin login that had been blocked all day. Built it as a genuine server-verified login (not a client-side password check) now that real serverless hosting exists — this is the first backend (non-static-file) part of this project.
- **Auth core** (`api/_lib/auth.js`): PBKDF2-SHA256 (210,000 iterations, fixed non-secret salt) password verification via `crypto.timingSafeEqual`; signed session cookie (payload + HMAC-SHA256 signature using a `SESSION_SECRET` env var), 12-hour expiry, `HttpOnly; Secure; SameSite=Strict`. No npm dependencies — everything uses Node's built-in `crypto`, keeping with the site's "no dependencies unless truly needed" approach. `api/admin/login.js`/`logout.js`/`session.js` wrap this. Unit-tested directly (mock req/res, no network): 16/16 checks passed, covering wrong method, missing/wrong/correct password, cookie flags, tampered/garbage/expired/no-secret tokens, and the full login→session-check→logout→session-check-again cycle.
- **GitHub write path** (`api/_lib/github.js`): thin wrapper over the GitHub Contents API (get file + sha, then PUT to commit), authenticated with an `ADMIN_GITHUB_TOKEN` env var (same kind of fine-grained, single-repo PAT already used for the daily automated session). Every write goes through this, same commit mechanism as this automated session already uses.
- **First real capability: show/hide any tool or blog post** (`api/_lib/cardVisibility.js` + `api/admin/visibility.js`). Retrofitted `<!-- card:ID -->...<!-- /card:ID -->` marker comments around all 13 cards in `index.html` (11 tools + 2 blog teasers) and all 4 cards in `blog/index.html` — purely mechanical, additive change (verified via diff and a full site-wide tag-balance check before touching anything else). Hiding a card never deletes its markup; it wraps the card in an HTML comment (`<!-- HIDDEN ... HIDDEN -->`) so showing it again is always a byte-for-byte perfect restore, never a re-generation. Caught and fixed a real bug during testing: blog-teaser cards are indented two spaces deeper than top-level tool cards, and an early version of the wrapper hardcoded indentation, which broke exact restoration for those — fixed by capturing and preserving each card's actual original whitespace instead of assuming one indent level. Verified with a pure-function unit test against the real `index.html`/`blog/index.html` content (not fixtures): hiding every one of the 17 cards one at a time then showing them all back again produces output byte-identical to the original file, for both files (12/12 checks). Also rendered the hidden-card output in a real headless Chromium page and confirmed the card is genuinely gone from the DOM (card count 11→10, zero console errors), not just commented in source.
- **Admin panel UI** (`admin/index.html`, not linked from site nav, `noindex`): password form → dashboard listing every tool/post from both listings with a live Show/Hide toggle per row, calling the API with the session cookie. Server-side `requireAuth` gates every write regardless of what the UI does, so the UI is a convenience layer, not where the real security lives.
- **Password setup helper** (`admin/setup-password.html`, also unlinked, `noindex`): computes the exact same PBKDF2 hash client-side via the Web Crypto API, so the owner's real password is never sent anywhere — not even to this site's own backend — to generate `ADMIN_PASSWORD_HASH`. Verified byte-for-byte in a real browser (Playwright/Chromium) that the client-side Web Crypto hash and the server-side Node `crypto.pbkdf2Sync` hash of the same password are identical — critical to check, since a mismatch here would lock the owner out entirely.
- **Full end-to-end test** before committing: a local Node server wiring the real handler files (login/logout/session/visibility) into plain `http`, with GitHub calls mocked via a stubbed `global.fetch` (no real repo writes during testing), driven through the actual `admin/index.html` UI in headless Chromium — login with wrong password (rejected, error shown), login with correct password (dashboard loads, 17 rows render), hide a card (UI updates, exactly one GitHub commit call, public homepage genuinely drops from 11 to 10 cards when reloaded), reload the admin panel (session persists, hidden state persists), show it again (public homepage back to 11 cards, `index.html` content byte-identical to the pre-hide original), log out (back to login view). 19/19 checks passed, zero unexpected console errors. Also re-ran the site-wide i18n key-coverage check (still zero missing keys) and an 18-page tag-balance + console-error smoke test across every page after the `index.html`/`blog/index.html` marker retrofit — nothing else broke.
- Added `ADMIN_SETUP.md` at the repo root with the owner's one-time setup steps (3 Vercel environment variables — `ADMIN_PASSWORD_HASH`, `SESSION_SECRET`, `ADMIN_GITHUB_TOKEN` — and how to generate each) and a plain-language explanation of how the login works. A ready-to-use random `SESSION_SECRET` was generated and given to the owner directly in chat (not committed to the repo, since it's a real secret).
- Explicitly scoped OUT of this v1 (documented as the next roadmap item, with a design already sketched out): adding blog posts through the panel, changing the accent color, and uploading a new logo. These were all part of the owner's original request but needed more design work to do safely (especially blog-post creation, which needs a way to add bilingual content without letting a public endpoint mutate the large shared `assets/i18n.js` file) — shipping the login + one fully-tested real capability now, rather than four capabilities without the same testing rigor, matches how this project has always preferred a smaller solid increment over a larger shaky one (see e.g. the PDF image-recompression caution across 2026-09-05 to 09-08).
- Not yet done, needs the owner: complete the `ADMIN_SETUP.md` steps in the Vercel dashboard (the panel returns 401/shows the login form until `ADMIN_PASSWORD_HASH` and `SESSION_SECRET` are set — untested against the actual live Vercel deployment since that requires the owner's own environment variables and account).

### 2026-09-14 — Chat: PDF↔Word request deferred, owner decided to migrate hosting (no code changes)
- Live chat with the site owner, no tool built this entry (updating ROADMAP.md/this log only, to preserve the decisions for future runs).
- Owner asked for a PDF↔Word converter "without messing up text or formatting." Explained the real constraint before building anything: DOCX→PDF is realistically doable client-side with good fidelity (mammoth.js + html2pdf.js, no backend); PDF→Word with true formatting/table/column fidelity is not achievable purely client-side without a real backend or heavy layout-inference work, and a naive text-only extraction would break the "don't mess up formatting" requirement, so didn't build a version that couldn't honestly meet the ask. Owner said to postpone it ("ناجلها لاحقا"). Logged full technical notes as a new `⚠️ NEEDS HUMAN`-adjacent item in ROADMAP.md so a future session (or the owner) can pick the right scope when it's revisited.
- Owner then asked again about the admin-login/control-panel request from earlier today (see the 2026-09-14 UUID-generator entry above) — re-explained the three options (do nothing extra / Decap CMS + OAuth proxy / migrate to real hosting), and clarified that color/logo changes, hiding/showing pages, and adding blog posts don't actually need a login at all — those can be requested directly in chat and shipped same-day, exactly like the tools added today.
- Owner then decided: **move the site to real hosting** (Netlify/Vercel/Cloudflare Pages — platform not chosen yet). Explained the site needs zero code changes to migrate (it's fully static already), and the recommended path is connecting the new host directly to this same GitHub repo so it auto-deploys on every push to `main` — meaning this automated session's daily workflow doesn't need to change at all, migration or not. Updated ROADMAP.md's admin-panel item to reflect this decision and what to do once the owner confirms a platform and it's live (then the real admin panel becomes a normal buildable roadmap item).
- No site-facing files changed, only ROADMAP.md and this log — committed and pushed so the decision persists for the next automated run.

### 2026-09-14 — 11th tool: Text Highlighter & Marker (owner chat request)
- The site owner asked directly in chat (Arabic): "ايضا قم بوضع اضافة رموز او تنصيص الى النص يتم وضع النص ووضع عليه علامات او تحديده الخ" — roughly "also add a way to mark up text: put in text and add marks to it or select/highlight parts of it, etc." The request didn't name an exact feature, so interpreted it as the most literal, common reading of "select text and mark it": a text highlighter/annotation tool, matching what "online text highlighter" tools typically offer. Stated that interpretation and built it directly, the same pattern used for the Base64 tool request on 2026-09-10.
- Added `tools/text-highlighter/index.html`: a `contenteditable` box where you type or paste text, select any part of it, and either click one of 5 highlight-color swatches or a Bold/Underline/Strikethrough button to mark the selection (via the browser's built-in `execCommand` text-editing commands — no library). A "Select some text first" guard shows if a formatting button is clicked with nothing selected, so it never fails silently. Two copy modes: "Copy formatted" writes both `text/html` and `text/plain` to the clipboard via `ClipboardItem` (falls back to plain-text `writeText` if `ClipboardItem` isn't supported) so pasting into an email/Google Doc/Word keeps the highlighting; "Copy as plain text" walks the edited HTML and converts it to plain text with markdown-style markers (`**bold**`, `__underline__`, `~~strikethrough~~`, `==highlight==`) for places that don't support rich text. Also has a "Remove formatting" button (clears marks from a selection) and "Clear all" (empties the box).
- Wired in everywhere new tools go: 11th card on `index.html`'s tool grid, URL added to `sitemap.xml`, reserved `.ad-slot`, canonical link, real meta description, two SEO paragraphs (how it works / when to use it). A small inline `<style>` block (still single-file, no separate CSS) handles the contenteditable placeholder text and the round color-swatch buttons, since those can't be done with inline `style=` attributes alone.
- Fully bilingual: all new `hl.*` and `home.card.hl.*` keys added to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic copy). The contenteditable placeholder isn't a real `placeholder` attribute (so the site's existing `data-i18n-placeholder` mechanism doesn't apply to it) — handled with a small custom `data-placeholder` attribute + CSS `:empty:before` rule, updated manually in JS on load and on the `qth-lang-changed` event. Verified with a Node/vm key-coverage script (433 keys in both dictionaries, all 368 `data-i18n*` keys used across all 21 HTML pages present in both languages, zero missing).
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a JS syntax check on the new page's inline script, a Python HTML tag-balance check (both new/changed files balanced), and a Playwright smoke test (with clipboard permissions granted) over a local `http.server` — verified selecting a word and applying Bold actually wraps it in `<b>`, selecting another word and clicking the yellow swatch adds a `background-color` span, clicking a format button with no selection shows the "select first" message instead of silently doing nothing, "Copy as plain text" produces exactly the expected `**bold**`/`==highlight==` markers in the real clipboard content, "Copy formatted" writes real `text/html` clipboard content containing the `<b>` tag and highlight span, Clear all empties the box, copying with an empty box shows the right error, and `#lang-toggle` flips to RTL Arabic with the H1 and the custom placeholder both translating — zero console errors throughout, including a spot-check of the homepage, About, UUID generator, Base64 converter and blog index pages.

### 2026-09-14 — 10th tool: UUID / GUID Generator
- Picked the next actionable ROADMAP item under "Next up": one of the two candidate developer-utility tools listed (UUID/GUID generator, Unix timestamp converter, HEX/RGB/HSL color converter) — built the UUID generator, leaving the other two as a follow-up item. (The PDF FlateDecode/raw-image recompression item at the top remains deferred for the same real-corruption-risk reason as every prior run.)
- Added `tools/uuid-generator/index.html`: generates random version-4 UUIDs via `crypto.randomUUID()` (with a manual `crypto.getRandomValues()`-based v4 fallback for older browsers), a quantity field (1-1000, clamped), and uppercase / no-hyphens / wrap-in-braces formatting checkboxes. Output is a plain textarea (one UUID per line) with Copy all and Clear buttons, so it doubles as a quick bulk-ID generator for test data.
- Wired in everywhere new tools go: 10th card on `index.html`'s tool grid, URL added to `sitemap.xml`, reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (what a UUID is / when you'd use one).
- Fully bilingual: all new `uuid.*` and `home.card.uuid.*` keys added to both `en`/`ar` dictionaries in `assets/i18n.js` (original Arabic copy, not machine-translated). Verified with a Node/vm key-coverage script (409 keys in both dictionaries, all 350 `data-i18n*` keys used across all 20 HTML pages present in both languages, zero missing).
- Sanity-checked before commit: `node --check` on `assets/i18n.js`, a JS syntax check (`new Function(...)`) on the new page's inline script, a Python HTML open/close tag-balance check on the changed/new files (both balanced), and a Playwright smoke test served over a local `http.server` — verified the default single-UUID generation on load, bulk generation of 5 unique UUIDs, the uppercase/no-hyphens/braces formatting combining correctly (`{F7234...}` pattern), the 1000-item clamp when an out-of-range quantity is entered, Copy/Clear, `#lang-toggle` flipping to RTL Arabic with the H1 and status text both translated, and the homepage card count (10) — zero console errors throughout, including on a spot-check of `about.html`, `tools/base64-converter/`, `tools/unit-converter/` and `blog/index.html`.
- Left a follow-up ROADMAP note: a Unix timestamp ↔ human-date converter and a HEX/RGB/HSL color converter remain good candidates for the next couple of "new tool" increments.

### 2026-09-14 — 4th blog article: "How to Generate a Strong Password"
- Picked the next actionable ROADMAP item under "Next up": one of the listed candidate long-tail articles, "how to generate a strong password" linking to the Password Generator tool. (The PDF FlateDecode/raw-image recompression item remains deferred at the top for the same real-corruption-risk reason as every prior run.)
- Added `blog/how-to-generate-a-strong-password/index.html` (~750 words): why length beats complexity, why reuse (not weak passwords) is the real risk in most breaches, a memorable passphrase technique, and how the site's Password Generator's crypto-secure random generation helps, plus a short practical checklist. Linked to `tools/password-generator/index.html`.
- Wired it in everywhere the existing 3 posts are: new card on `blog/index.html` (now 4 posts), replaced the oldest card in the homepage "From the blog" two-card strip with it (now shows posts 3 and 4, the two most recent), and added it to `sitemap.xml`.
- Fully bilingual: added `blogpost4.*` (25 keys) and `blog.post4.*` (4 keys) to both `en`/`ar` dictionaries in `assets/i18n.js`, written in the same register/dialect as the existing Arabic blog posts. Verified with a Node/vm key-coverage script (333 keys used across all 19 HTML pages now, zero missing in either language).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on `assets/i18n.js`, an HTML open/close tag-balance check on the new and changed files, and a Playwright smoke test served over a local `http.server` — verified the new post's title/h1 render, `#lang-toggle` flips to RTL Arabic with the CTA link translated, the blog index shows 4 cards with the new one linking correctly, and the homepage strip shows 2 cards — zero console errors throughout.
- **Also addressed (without building) an out-of-band chat request from the site owner** (2026-09-14, in Arabic) for an admin login page, username/password change, a full control-panel dashboard (colors, page edits), and blog-post management through that panel. Did not build this: a password check living in client-side JS on a static GitHub Pages site isn't real security (view-source defeats it instantly), so a fake login would be actively misleading rather than protective, and a genuinely secure version needs either a Git-based CMS with real GitHub OAuth (Decap/Netlify CMS, which needs a small OAuth-proxy backend hosted somewhere other than GitHub Pages) or moving the site to a platform with real serverless backend support (Netlify/Vercel/Cloudflare Pages) — both are hosting/account decisions only the site owner can make. Added a detailed `⚠️ NEEDS HUMAN` entry to `ROADMAP.md` laying out three concrete options (do nothing extra since this automated session already adds posts via direct commits; stand up Decap CMS + a free OAuth proxy; or migrate hosting) so the owner can pick a direction next time.

### 2026-09-12 — "From the blog" teaser section on the homepage
- Picked the next actionable ROADMAP item under "Next up": add a homepage teaser for the blog, now that there are 3 posts live. (The PDF FlateDecode/raw-image recompression item at the top remains deferred for the same reasons as every prior run — real risk of corrupting a user's PDF, too big for a single modest increment.)
- Added a "From the blog" section to `index.html`, right after the existing "Why Code Engineer?" content block: a two-card mini-grid (reusing the existing `.grid`/`.card` styles) linking to the two most recent posts — "How to Make a QR Code for a Wi-Fi Password" and "Why Is My JSON Invalid?" — plus a "View all posts →" link to `blog/index.html`. No new tool, so no sitemap change needed (homepage URL is already listed).
- Fully bilingual: added `home.blog.h2` and `home.blog.viewall` keys to both `en`/`ar` dictionaries in `assets/i18n.js`; the two post titles/descriptions/dates reuse the existing `blog.post2.*`/`blog.post3.*` keys already defined for `blog/index.html`, so no duplicate translation needed.
- Verified before committing: a Node script diffed every `data-i18n*` key used across all `.html` files against the `en`/`ar` dictionaries in `assets/i18n.js` — zero missing keys. `node --check` passed on `assets/i18n.js`, div tags balanced in `index.html`. Ran a Playwright headless-Chromium smoke test on the updated `index.html`: the new section renders with exactly 2 cards pointing at the correct relative blog URLs, `#lang-toggle` flips `dir` to `rtl` and swaps the new section's heading to Arabic, and no console/page errors.

### 2026-09-11 — Third blog post: "Why Is My JSON Invalid?"
- Picked the next actionable ROADMAP item under "Next up": a third long-tail blog article linking to an existing tool, per the roadmap's suggested candidate. Deferred the top item again first (the PDF FlateDecode/raw-image recompression follow-up), for the same reason noted on every prior run since 2026-09-05: it's real binary-format work with a genuine risk of corrupting a user's PDF, doesn't fit a single modest well-verified increment, and isn't first in the actionable queue once you skip past it to the next independent task.
- Wrote `blog/why-is-my-json-invalid/index.html`, a ~700-word guide covering the seven most common reasons JSON fails to parse (trailing commas, single vs. double quotes, unquoted keys, comments, unescaped special characters, JS-only values like `undefined`/`NaN`, and mismatched brackets), each with a concrete broken example and a one-line fix, ending with a link to `tools/json-formatter/index.html`. No tool code changed — the JSON Formatter already surfaces line/column on parse errors, which the article references.
- Added the new post's card to `blog/index.html`'s grid (now 3 posts) and its URL to `sitemap.xml`. Fully bilingual: all new `blog.post3.*` and `blogpost3.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js` (Arabic is an original translation, not machine-translated boilerplate, matching the tone of the existing two posts).
- Sanity-checked before commit: a Node/vm key-coverage script (362 keys in both dictionaries, all 307 `data-i18n*` keys used across all 18 HTML pages present in both languages, zero missing), a Python HTML open/close tag-balance check on the new and changed files (both OK), a JS syntax check (`new Function(...)`) on `assets/i18n.js`, and a Playwright smoke test over a local `http.server` — the blog index and new post both load with zero console errors, `#lang-toggle` flips to RTL Arabic with the translated `<h1>` showing, the blog index's card count is now 3, and navigation end-to-end (blog index → new post card → article → "Try the free JSON Formatter & Validator" CTA → `tools/json-formatter/index.html`) all resolves correctly.
- Left updated ROADMAP notes: marked this article done, flagged the homepage "Blog" teaser card item as now ready to do (3 posts exist), and refreshed the future-article candidate list (a CSV↔JSON idea needs a first check on tool fit before writing, per the roadmap's own HEIC lesson from 2026-09-10).

### 2026-09-10 — 9th tool: Base64 Encoder & Decoder
- The site owner asked directly (in chat) for a new tool right after declining the file-upload-to-link idea above. Shipped a 9th tool: Base64 Encoder & Decoder (`tools/base64-converter/index.html`) — a text box with Encode/Decode buttons (UTF-8-safe via `TextEncoder`/`TextDecoder` + `btoa`/`atob`, so non-English text round-trips correctly), plus a second panel that reads any local file with `FileReader.readAsDataURL` and shows its Base64 data URI for copying (useful for embedding small images/icons in CSS or HTML). Reused the site's existing `.dropzone` component for the file picker for visual consistency with the Image Converter/Resizer.
- Fully bilingual: all new `home.card.b64.*` and `b64.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`; verified with the Node/vm key-coverage script (295 keys used across all 16 HTML pages, zero missing in either language).
- Linked from the homepage grid (9th card) and added to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how Base64 works + when you'd use it).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on the inline script and `assets/i18n.js`, an HTML tag-balance check across all pages, and a Playwright smoke test over a local `http.server` — verified a full text round trip including Arabic/Unicode content, the empty-input and invalid-Base64 error messages, a real file → data-URI → decode-back round trip (byte-for-byte match), the homepage card count (9) and click-through, and `#lang-toggle` flipping to RTL Arabic with the title translated — zero console errors throughout.
- Left three more well-scoped developer-tool ideas on ROADMAP.md for future increments (UUID generator, Unix timestamp converter, HEX/RGB/HSL color converter).

### 2026-09-10 — Second blog post: QR code for a Wi-Fi password
- Picked the next ROADMAP item under "Next up": another long-tail blog article linking to an existing tool. Wrote `blog/qr-code-for-wifi-password/index.html`, a ~600-word guide explaining the standard `WIFI:T:...;S:...;P:...;;` QR text format that phone cameras (iOS 11+/Android 10+) recognize natively, how to build the string (security type, SSID, password, escaping special characters), a hidden-network (`H:true;`) note, and a plain-text-password security caveat — ending with a link to `tools/qr-generator/index.html`, which already accepts arbitrary text so no tool code changed.
- Considered the other roadmap candidate ("how to convert HEIC to JPG for free" → image-converter) and deliberately skipped it: the Image Converter decodes images via the browser's native `<img>`/canvas pipeline, and most non-Safari browsers can't decode HEIC at all, so that article would send readers to a tool that silently fails on their exact use case. Left a note on ROADMAP.md explaining why, so a future run doesn't write it without also fixing the tool first.
- Added the new post's card to `blog/index.html`'s grid, added the URL to `sitemap.xml`. Fully bilingual: all new `blog.post2.*` and `blogpost2.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js` (Arabic is an original translation matching the existing article's tone, not machine-translated boilerplate).
- Sanity-checked before commit: a Node/vm key-coverage script (294 keys in both dictionaries, all 249 `data-i18n*` keys used across every HTML page present in both languages, zero missing), a Python HTML open/close tag-balance check across all pages (all OK), a JS syntax check on `assets/i18n.js`, and a 3-page Playwright smoke test over a local `http.server` — the blog index and new post both load with zero console errors and `#lang-toggle` flips to RTL correctly; the QR generator page shows its pre-existing, sandbox-only jsdelivr-CDN-blocked error (not a regression, noted in earlier entries). Also verified navigation end-to-end: blog index → new post card → article page, and the article's CTA link resolves to `../../tools/qr-generator/index.html`.

### 2026-09-10 — Feature request evaluated and declined: file-upload-to-QR/link tool
- The site owner asked (in a live chat, not the daily automated run) for a tool to upload any PDF or image and get back a QR code or link, valid for a set duration or forever. Explained the architectural conflict: making a link resolvable later means the file must be stored on some server, which breaks every other tool's "nothing leaves your browser" promise. Looked into three options (encode the file straight into the QR — only feasible for tiny data, not real PDFs/images; call a free anonymous host like catbox.moe/litterbox.catbox.moe directly from browser JS — found no evidence these support direct browser CORS uploads, only CLI/server tools, so it likely silently fails for real visitors, and it would still mean visitor files sit on a third party's server; or stand up a real backend — a genuine infrastructure change, not a static-site increment) and presented the trade-offs.
- Owner declined outright specifically on privacy grounds. Logged this as a rejected direction in ROADMAP.md's new "Declined by site owner" section so future automated runs don't re-propose it or a variant that stores/hosts uploaded files.
- No site files changed other than ROADMAP.md/this log entry.

### 2026-09-09 — First blog post + site-wide "Blog" nav link
- Deferred the top ROADMAP item again (PDF FlateDecode/raw-image recompression follow-up) for the same reason it was pushed back on 2026-09-05/06/07: it's real binary-format work with a genuine risk of corrupting a user's PDF, and doesn't fit a single modest, well-verified increment. Picked the next item instead: the first `/blog/` article, which is the main lever the roadmap identifies for organic search traffic.
- Added `blog/index.html` (a simple post-listing page, same header/footer/card-grid pattern as the homepage) and the first post, `blog/how-to-compress-a-pdf-without-losing-quality/index.html` — a ~650-word, genuinely useful guide (why PDFs get big, a 3-step walkthrough, an honest explanation of what the compressor's JPEG recompression does and doesn't touch, plus extra tips) targeting the long-tail query named in the roadmap, ending with a link to `tools/pdf-compressor/`.
- Added a "Blog" link to the header nav on all 13 existing pages (root-relative `./blog/index.html` on root pages, `../../blog/index.html` on tool pages) plus the new `nav.blog` i18n key, so the new section is discoverable site-wide, not just from a card somewhere.
- Both new pages are fully bilingual (all `blog.*` and `blogpost1.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`, including a full Arabic translation of the article body) and include a reserved `.ad-slot`, a canonical link, and real meta title/description. Added both new URLs to `sitemap.xml`.
- Sanity-checked before commit: a Node/vm key-coverage script (266 keys in both dictionaries, all 222 keys used across the now-15 HTML pages present in both languages, zero missing), a Python HTML open/close tag-balance check across all pages (all OK), and a 15-page Playwright smoke test over a local `http.server` — every page loads with zero console errors except the QR generator and PDF Compressor's pre-existing, sandbox-only jsdelivr-CDN-blocked error (not a regression, noted in the 2026-09-08 entry). Also verified end-to-end: homepage → Blog nav link → blog listing → article card → article page → "Try the free PDF Compressor" CTA all navigate correctly, and `#lang-toggle` on the new article flips to RTL Arabic with the translated title showing.
- Left follow-up ROADMAP notes: 2-3 more long-tail article ideas tied to specific existing tools, and adding a homepage teaser card for the blog once there are a few posts.

### 2026-09-08 — PDF Compressor v2: embedded JPEG recompression (the item deferred three times)
- Tackled the top ROADMAP item that had been deliberately skipped on 2026-09-05, 2026-09-06 and 2026-09-07 for being too risky to ship without a way to verify it first. Built the pdf.js-based before/after render-diff harness the previous notes asked for (as a *development-time* test harness, not shipped to production — see below for why), used it to validate the approach against several synthetic test PDFs, then implemented the feature in `tools/pdf-compressor/index.html`.
- What it does: after stripping metadata (v1 behavior, unchanged), the tool now walks each page's XObject resources looking for images already stored as JPEG (`Filter=/DCTDecode`), and recompresses eligible ones through the browser's own canvas (`createImageBitmap` → draw → `canvas.toBlob('image/jpeg', 0.72)`), replacing the embedded stream only when the result is actually smaller. This is the biggest lever for file size since embedded images (not metadata) dominate most real-world PDFs.
- Safety guards, since a corrupted download from a "PDF compressor" would be worse than no feature at all: (1) parses the real JPEG SOF marker to get the true component count and skips anything that isn't 1 (grayscale) or 3 (RGB/YCbCr) — CMYK/YCCK JPEGs are left untouched, since browsers don't decode those correctly; (2) skips images with a `Decode` array, `DecodeParms`, or an Indexed/Separation/DeviceN color space, since re-encoding could silently change the color mapping; (3) verifies the decoded bitmap's dimensions match the PDF's declared Width/Height before trusting it; (4) only ever replaces a stream if the new bytes are strictly smaller; (5) after saving, reloads the produced PDF with pdf-lib and checks the page count survived — if that check fails for any reason, it re-saves a metadata-only version from the original bytes instead and ships that, so a broken image edit can never reach the user's download.
- Verification approach (done in a scratch `/tmp` directory, not committed — this is testing methodology, not a repo change): generated synthetic test PDFs with Python/PIL + a Node pdf-lib script covering RGB JPEG, grayscale JPEG, CMYK JPEG, a progressive JPEG, an already-heavily-compressed JPEG, a JPEG with a hand-added `Decode` array, a PDF with no images, and a PDF reusing the same image XObject across three pages. Ran the real recompression code in headless Chromium (Playwright) against each: CMYK and the Decode-array image were correctly left byte-identical (0.0 pixel diff via a pdf.js page-render comparison), RGB/gray/progressive images shrank 33-85% with an average per-channel pixel diff of 0.5-4.1 out of 255 (visually indistinguishable), the shared-image case deduplicated correctly (compressed once, applied to all 3 pages), and every produced PDF reloaded cleanly with the correct page count. Then repeated the RGB-image test against the actual production `tools/pdf-compressor/index.html` file (served locally, with the jsdelivr `pdf-lib` request intercepted and served from a local copy of the same library version, since this sandbox's network blocks jsdelivr outright — same workaround used for the 2026-09-04 PDF Compressor v1 test) and confirmed the real UI flow end-to-end: 1160.7 KB → 818.4 KB (29% smaller, 2 images optimized), zero console errors, `#lang-toggle` still flips to RTL correctly.
- Decided *not* to ship pdf.js itself to production alongside pdf-lib — it's a heavy dependency (worker file, ~1-2 MB) that would roughly double this page's payload for every visitor, and the safety guards above (component-count check, color-space checks, dimension check, reload-and-verify, automatic fallback) give strong correctness guarantees without it. pdf.js stayed a one-off verification tool for this session.
- Updated the tool's copy (`pdf.desc`, `pdf.note`, `pdf.p.how`, meta description) and added a `pdf.summary.images` i18n key to reflect the new behavior honestly, in both `en` and `ar`. Verified with the Node/vm key-coverage script (237 keys in both dictionaries, all 194 keys used across the 13 HTML pages present in both languages, zero missing).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on the tool's inline script and `assets/i18n.js`, an HTML tag-balance check on the changed file, and a full 13-page Playwright smoke pass confirming every other page still loads with zero console errors (the QR generator and PDF Compressor both show the expected, pre-existing jsdelivr-CDN-blocked error in this sandbox only — not a regression, and not something real site visitors will see).
- Left a smaller, well-scoped follow-up on ROADMAP.md: this version only recompresses images already stored as JPEG; raw/FlateDecode pixel data (not JPEG-encoded) is left untouched and could be a future increment using the same safety pattern.


### 2026-09-07 — 8th tool: Unit Converter
- Shipped an 8th tool: Unit Converter (`tools/unit-converter/index.html`) — a category dropdown (Length / Weight / Temperature), a "From" number input + unit select, a "To" number input + unit select, and a Swap button. Typing in either number field recalculates the other live, in both directions, and switching category repopulates both unit dropdowns with sensible defaults (cm→in, kg→lb, °C→°F). Length and weight convert through a shared base unit (meters/grams) with exact fixed conversion factors (mm, cm, m, km, in, ft, yd, mi / mg, g, kg, oz, lb); temperature uses the real Celsius/Fahrenheit/Kelvin formulas rather than a linear factor, since those scales don't share a zero point.
- Picked this over the top ROADMAP item (PDF image-recompression via pdf-lib XObjects) for the third time this round, for the same reason noted on 2026-09-05 and 2026-09-06: that change needs careful embedded-image color-space/filter handling and real risk of corrupting PDFs without a way to visually verify output in this sandbox. Left an updated note on that ROADMAP item suggesting a future run build a pdf.js before/after page-render diff harness first, as a safety net, before attempting the XObject recompression.
- Fully bilingual: all new `unit.*` and `home.card.unit.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`; verified with the Node/vm key-coverage script (194 keys used across all 13 HTML pages, zero missing in either language). Unit symbols themselves (mm, kg, °C, etc.) are kept as-is in both languages, consistent with the existing convention for locale-neutral tokens like `json.indent.tab`.
- Linked from the homepage grid (new card, 8th tool) and added to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how the conversion math works + when you'd use a unit converter).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on the inline script and `assets/i18n.js`, an HTML open/close tag-balance check, and a Playwright smoke test served over a local `http.server` — verified exact conversion values in both directions (1 cm → 0.393701 in, 100 cm → 39.370079 in, 1 kg → 2.204623 lb, 100°C → 212°F, 32°F reverse-computed → 0°C), category switching resets units to sane defaults, the Swap button swaps unit selections, and `#lang-toggle` flips to RTL Arabic with all visible text translated — zero console errors throughout. Also confirmed the homepage card count (8) and that the new card links through correctly.

### 2026-09-06 — 7th tool: JSON Formatter & Validator
- Shipped a 7th tool: JSON Formatter & Validator (`tools/json-formatter/index.html`) — a textarea for pasting JSON, a Format button (pretty-prints via `JSON.stringify(JSON.parse(input), null, indent)` with a selectable 2-space/4-space/tab indent), a Minify button (single-line output), and a Copy/Clear pair. Invalid JSON shows a plain-English status message; when the browser's `JSON.parse` error exposes a position or line/column (format varies by engine — handled both), the message names the approximate line and column so the user doesn't have to scan the whole document by eye.
- Picked this over the top ROADMAP item (PDF image-recompression via pdf-lib XObjects) again this round, for the same reason noted on 2026-09-05: that item needs careful embedded-image color-space handling and is hard to visually verify without a PDF renderer in this sandbox. Left a note on that ROADMAP item suggesting a future run specifically budget time to render before/after PDF pages via pdf.js for a visual diff, rather than deferring a third time.
- Fully bilingual: all new `json.*` and `home.card.json.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`; verified with the Node/vm key-coverage script (176 keys used across all 12 HTML pages, zero missing in either language).
- Linked from the homepage grid (new card, 7th tool) and added to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how the parsing/formatting works + when you'd use a JSON formatter).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on the inline script and `assets/i18n.js`, an HTML open/close tag-balance check, and a Playwright smoke test served over a local `http.server` — verified Format (including the 4-space indent option) and Minify both round-trip valid JSON correctly, invalid JSON shows the expected error (with line/column when the engine's error message provides one, a generic fallback message otherwise), the empty-input case is handled, Copy/Clear work, and `#lang-toggle` flips to RTL Arabic with all visible text translated — zero console errors throughout. Also confirmed the homepage card count (7) and that the new card links through correctly.
- Added a new ROADMAP item for an 8th tool (Unit Converter: length/weight/temperature) as the next well-scoped increment.

### 2026-09-05 — 6th tool: Image Resizer
- Shipped a 6th tool: Image Resizer (`tools/image-resizer/index.html`) — a dedicated, simpler UI than the Image Converter: width/height number inputs with an aspect-ratio-lock checkbox (editing one field auto-recalculates the other), an original-image preview on upload, and a before/after comparison (dimensions + file size) after resizing. Uses the same canvas draw/re-encode approach as the Image Converter; downloads in the original file's format (JPEG/PNG/WebP detected from the source file, defaulting to PNG for anything else) with quality 0.92 for lossy formats.
- Deliberately picked this over the top ROADMAP item (PDF image-recompression via pdf-lib XObjects) this round: that item needs careful embedded-image color-space/filter handling and is hard to visually verify without a PDF renderer available in this sandbox, so it stays queued at the top of ROADMAP.md with a note, to be tackled with more care in a future run rather than rushed.
- Fully bilingual: all new `resize.*` and `home.card.resize.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`; verified with a Node/vm key-coverage script (159 keys used across all 11 HTML pages, zero missing in either language).
- Linked from the homepage grid (new card, 6th tool) and added to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how it works + when to resize an image).
- Sanity-checked before commit: JS syntax check (`new Function(...)`) on the inline script and `assets/i18n.js`, an HTML open/close tag-balance check on the new and changed files, and a Playwright smoke test served over a local `http.server` — uploaded a generated test PNG, verified the aspect-ratio lock recalculates height from width, clicked Resize and confirmed the before/after summary and blob-URL download wiring, and confirmed `#lang-toggle` flips to RTL Arabic — all passed with zero console errors. Also smoke-tested the homepage: card count, new card renders and links through to the tool page correctly.

### 2026-09-04 — 5th tool: PDF Compressor
- Shipped a 5th tool: PDF Compressor (`tools/pdf-compressor/index.html`), the first roadmap item under "Next up". v1 uses `pdf-lib` via CDN (`https://cdn.jsdelivr.net/npm/pdf-lib/dist/pdf-lib.min.js`) client-side: loads the uploaded PDF, strips embedded metadata (title, author, subject, keywords, producer, creator), and re-saves it with compact cross-reference object streams. Shows a before/after page-count and file-size comparison and a download button. Scoped deliberately to metadata-stripping + object-stream re-save per the roadmap's note that image recompression could wait for a later increment — added that as a new roadmap follow-up item.
- Fully bilingual: all new `pdf.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`; verified with the Node/vm key-coverage script (139 keys used across all pages, zero missing in either language).
- Homepage: replaced the "PDF Compressor — coming soon" placeholder card with a live link, badge changed to "Live". Added the tool to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how the compression works + tips for shrinking PDFs further).
- Sanity-checked before commit: JS syntax check (`new Function(...)` on the inline script and `assets/i18n.js`), HTML tag-balance check on the new and changed files, and a Playwright smoke test — since this sandbox's network blocks the jsdelivr CDN outright (unrelated to whether it'll work for real site visitors), the test intercepted the `pdf-lib.min.js` request with a same-API stub to exercise the real UI flow: file upload, Compress click, before/after result rendering, download-button wiring, and `#lang-toggle` flipping to RTL Arabic — all passed with zero console errors.

### 2026-09-03 — 4th tool: Password Generator
- Shipped a 4th tool: Password Generator (`tools/password-generator/index.html`) — length slider (4-64 chars, live-updating), checkboxes for uppercase/lowercase/numbers/symbols, an "exclude similar characters" option (l, 1, I, O, 0), a Weak/Fair/Good/Strong strength indicator, and copy-to-clipboard. Passwords are generated with `crypto.getRandomValues()` (Web Crypto API), not `Math.random()`.
- Fully bilingual: all new `pwd.*` and `home.card.pwd.*` keys added to both `en` and `ar` dictionaries in `assets/i18n.js`, including RTL-correct Arabic copy; verified with the Node/vm key-coverage script (125 keys used across all pages, zero missing in either language).
- Linked the new tool from the homepage grid (replacing its slot before the "PDF Compressor — coming soon" card) and added it to `sitemap.xml`. Includes a reserved `.ad-slot`, canonical link, real meta description, and two SEO paragraphs (how it works + password-strength tips).
- Sanity-checked before commit: JS syntax check (`new Function(...)` on the inline script and `assets/i18n.js`), HTML tag-balance check, and a Playwright smoke test (page loads with zero console errors, Generate/length-slider/checkbox validation/strength meter all work, `#lang-toggle` correctly flips to RTL Arabic and swaps all visible text).

### 2026-09-02 — Rebrand to "Code Engineer" / "مهندس كود" + real logo
- Site owner supplied a logo image ("مهندس كود / CODE ENGINEER", a gear+dollar-sign mark). Cropped it down to just the circular gear icon (transparent background) and generated `assets/logo-icon.png` plus `favicon.ico` / `favicon-32.png` / `favicon-180.png` / `favicon-512.png`, linked from every page's `<head>`.
- Renamed the brand everywhere from "QuickToolsHub" to **"Code Engineer"** (English) / **"مهندس كود"** (Arabic) via a new `brand.name` i18n key — header logo+name, footer copyright, all page titles/meta, README. The name now correctly switches with the language toggle (verified: browser tab title and header text both flip EN↔AR on `#lang-toggle`).
- Re-ran the i18n key-coverage check, JS syntax check, tag-balance check, and a Playwright smoke test (logo image loads, title/brand swap language, no console errors, RTL header layout looks correct) before committing.


### 2026-09-02 — Bilingual (EN/AR) support + image tool preview/compare
- Added `assets/i18n.js`: a small client-side i18n system (English/Arabic dictionary, `data-i18n`/`data-i18n-content`/`data-i18n-placeholder` attributes, `#lang-toggle` button, persists choice in `localStorage`, flips `<html dir>` to `rtl` for Arabic). Applied it across every existing page (homepage, about, contact, privacy, terms, and all 3 tools).
- Validated coverage with a Node script that loads `assets/i18n.js` in a `vm` sandbox and diffs every `data-i18n*` key found across the `.html` files against the `en`/`ar` dictionaries — zero missing keys.
- Smoke-tested every page with Playwright (headless Chromium): page loads with no console errors, `#lang-toggle` correctly flips `dir="rtl"` and swaps visible text.
- Image Converter & Compressor: now shows an original-image preview (thumbnail, dimensions, file size, format) immediately after upload, and after conversion shows a side-by-side before/after comparison plus a "what changed" summary line (format, quality, dimensions, file size delta with %), fully translated and re-rendered on language toggle.
- Added an i18n convention + a verification checklist to `ROADMAP.md` so future tools (built by the daily automated session) stay bilingual and get sanity-checked before commit.

### 2026-09-02 — Site went live + 3rd tool shipped
- Repo made public and GitHub Pages enabled (Deploy from branch: `main` / root). Site confirmed live at https://fantasiacod.github.io/username-free-tools-site/.
- Shipped a 3rd tool: Text Case Converter & Word Counter (`tools/text-case-converter/index.html`) — UPPERCASE/lowercase/Title Case/Sentence case buttons, copy/clear, and a live word/character/sentence counter. Pure JS, no dependencies.
- Linked the new tool from the homepage grid and added it to `sitemap.xml`.

### 2026-09-02 — Initial scaffold
- Created the site from scratch: homepage (`index.html`), shared stylesheet (`assets/style.css`).
- Built two fully working tools: QR Code Generator (`tools/qr-generator/`) and Image Converter & Compressor (`tools/image-converter/`).
- Added policy/trust pages required for future ad network approval: `about.html`, `contact.html`, `privacy-policy.html`, `terms.html`.
- Added `robots.txt`, `sitemap.xml`, `.nojekyll`.
- Set up `ROADMAP.md` (task queue) and this `PROGRESS.md` (log) as the persistent memory for the daily automated session.
- Pushed to `main` on GitHub. GitHub Pages needs to be enabled once in repo settings if not already (see ROADMAP "NEEDS HUMAN").
